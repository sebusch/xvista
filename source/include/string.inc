C       Define a common block for storing strings.

	INTEGER         MAXNUMSTRINGS
	PARAMETER       (MAXNUMSTRINGS = 200)

	CHARACTER*4000  STRINGVALUES(MAXNUMSTRINGS)
	CHARACTER*8     STRINGNAMES(MAXNUMSTRINGS)

	COMMON /VISTAVARVALUES/ STRINGVALUES, STRINGNAMES

